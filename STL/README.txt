File description:

train.py is used for performing STL on a single
dataset(or a combined datasets within the same task)

How to use:
(belows scripts are just examples, please see the py files for documentation):

First download the data(see )

Then run the code:
  python train.py --model 36 --single ?  #'?' need to be replaced by actual argument

                                           
